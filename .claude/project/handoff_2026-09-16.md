# Handoff Summary — 2026-09-16 15:46 (Asia/Saigon)

## 1. Tình trạng hiện tại (1 câu)
> Phase 1A — review đã mở lại Quality Gate 1 vì corpus chunk hiện tại chưa đáng tin cậy; thiết kế tổng thể đã được giữ theo cấu trúc gốc và phần triển khai chi tiết được tách theo phase.

## 2. Việc đã làm trong session này

### Hoàn tất ✅
- Đọc `Project.md`, `technical_design.md`, `operations_guide.md`, `session_state.md` và kiểm tra mã, dữ liệu Phase 1.
- Chạy 10 unit tests hiện có (đều pass), audit 107 DOCX, 102 Markdown, 103 cleaned JSON, 2.041 chunks và relations; lưu bằng chứng tại `reports/phase1-review-2026-09-16/` cùng script chỉ đọc `scripts/audit_phase1.py`.
- Kết luận Gate 1 FAIL: chỉ có 1.969 chunk IDs duy nhất; BLLĐ 2019 vắng trong `chunks.jsonl`; 1.594 chunks thiếu effective_date; toàn bộ 2.041 chunks thiếu source URL; numbering, annex/form và relations có lỗi ảnh hưởng trích dẫn/hiệu lực.
- Giữ `technical_design.md` v1.3 như thiết kế tổng thể từ đầu, chỉ đính chính lỗi đã xác minh. Tách đặc tả triển khai vào `phase1_design.md` và `phase2_design.md`; lưu bản mở rộng v1.2 làm tham khảo ở `reports/phase1-review-2026-09-16/design_review_v1.2.md`.
- Cập nhật operations guide, open issues, bootstrap và session state. Chốt baseline đo retrieval đơn giản: BM25 → BGE-M3 dense → hybrid RRF; chưa cần reranker cho lần đo đầu.

### Đã bắt đầu nhưng chưa xong 🟡
- Phase 1A mới có review, audit và thiết kế; chưa sửa production ingestion hoặc rebuild corpus. Chưa đo retrieval trên corpus được duyệt.

## 3. Việc cần làm TIẾP THEO (ưu tiên cao → thấp)

1. **Xác minh nguồn và định danh văn bản** — Agent: Ingestion Agent → Reviewer Agent.
   - Đối chiếu registry, nguồn chính thức, file DOCX trùng tên, tên/số hiệu BHYT và coverage; truy nguyên vì sao BLLĐ 2019 có cleaned JSON nhưng không có trong `chunks.jsonl`.
   - Input: `data/raw/`, `data/cleaned/`, `data/chunks.jsonl`, audit và `phase1_design.md`.
   - Output: source registry/manifest đã xác minh, danh sách giữ/quarantine và coverage kiểm tra được.
2. **Sửa parser/chunker và rebuild dữ liệu** — Agent: Ingestion Agent → Reviewer Agent.
   - Khôi phục Word numbering, tách body khỏi annex/form/quoted amendments, định danh chunk duy nhất và provenance/temporal metadata; rebuild deterministic, atomic.
   - Input: nguồn đã xác minh và thiết kế Phase 1; output: corpus/schema v2 cùng báo cáo validation toàn bộ và mẫu so với nguồn.
3. **Duyệt relations và đóng Gate 1** — Agent: Ingestion Agent + Eval/QA Agent → Reviewer Agent.
   - Quan hệ regex chỉ là candidate; xác minh evidence, ngày hiệu lực và scope ở cấp điều/khoản trước khi dùng lọc hiệu lực. Đóng các lỗi Critical, chạy lại audit và nghiệm thu Gate 1.
4. **Đo baseline retrieval sau Gate 1** — Agent: RAG Engineer Agent + Eval/QA Agent.
   - Lập gold set, đo BM25, BGE-M3 dense và hybrid RRF theo `phase2_design.md`; chỉ thêm reranker hoặc nâng độ phức tạp sau khi có số liệu lỗi.

## 4. Quyết định quan trọng đã chốt trong session này

| Quyết định | Lý do |
|---|---|
| Giữ technical design tổng thể; tách phase design | Người dùng muốn một bản kiến trúc xuyên suốt từ đầu và đặc tả files/agents riêng mỗi phase. |
| Gate 1 mở lại | Tests hiện có pass nhưng audit corpus phát hiện lỗi nguồn, định danh, cấu trúc, thời gian và quan hệ. |
| Chunking theo Điều vẫn là chiến lược nền, chưa thể bảo đảm cho tương lai | Cần parser đúng cấu trúc, chunk ID duy nhất, provenance, phiên bản theo hiệu lực và kiểm định; một quy tắc cắt chunk không tự bảo đảm trích dẫn đúng. |
| Đo baseline đơn giản trước | Cần phân biệt lỗi dữ liệu với lỗi retrieval, tránh tối ưu model khi corpus chưa đạt. |

## 5. Vấn đề / Blocker cần giải quyết

- BLLĐ 2019 thiếu trong chunks; định danh BHYT và DOCX trùng tên chưa được xác minh.
- Word numbering, phụ lục/biểu mẫu và quoted amendments có thể làm sai ranh giới điều khoản; có chunk cực lớn 267.523 ký tự.
- 36 nhóm trùng chunk ID (72 dòng thừa), 1.594 chunks thiếu effective_date, mọi chunk thiếu source URL; 95 relations có 29 cần review, phần lớn thiếu ngày.
- File handoff cũ bên dưới ghi Gate 1 PASS và ưu tiên Qdrant; đó là trạng thái lịch sử đã bị review này thay thế.

## 6. Tài nguyên liên quan

| Tài liệu | Đường dẫn | Lý do liên quan |
|---|---|---|
| Bootstrap | `.claude/CLAUDE.local.md` | Phase và quy tắc khởi động phiên mới |
| Session state | `.claude/project/session_state.md` | Trạng thái task và quyết định hiện hành |
| Tổng thể | `.claude/project/technical_design.md` | Kiến trúc xuyên suốt, v1.3 |
| Phase 1 | `.claude/project/phase1_design.md` | Contract và triển khai sửa dữ liệu |
| Phase 2 | `.claude/project/phase2_design.md` | Baseline retrieval sau Gate 1 |
| Review và audit | `reports/phase1-review-2026-09-16/review.md`, `audit.json` | Findings và bằng chứng định lượng |
| Issues | `.claude/project/open_issues.md` | Blockers cần xử lý |

## 7. Prompt để bootstrap conversation mới

Đọc `.claude/CLAUDE.local.md`, `.claude/project/session_state.md`, `.claude/project/handoff_2026-09-16.md` (phần handoff 15:46 ở đầu; phần 01:50 là lịch sử), sau đó đọc `reports/phase1-review-2026-09-16/review.md` và `.claude/project/phase1_design.md`. Tiếp tục Phase 1A từ xác minh source registry/identity và coverage, rồi sửa parser/chunker; không coi Gate 1 đã pass hoặc index corpus cũ vào Phase 2. Giữ `technical_design.md` là thiết kế tổng thể, chỉ sửa khi có vấn đề được xác minh.

---

# Handoff lịch sử — 2026-09-16 01:50 (đã bị review mới thay thế về trạng thái Gate 1)

## 1. Tình trạng hiện tại (1 câu)
> Phase 1 — Data Foundation. Vừa hoàn tất: Phân tích và trích xuất thành công 104 mối quan hệ pháp luật bằng Regex. Đang dở: Chuyển giao sang Phase 2 (Retrieval Pipeline).

## 2. Việc đã làm trong session này

### Hoàn tất ✅
- Tạo script `extract_relations.py` → Kết quả: `data/document_relations.json` (104 quan hệ), `data/relations_needs_review.json` (29 quan hệ rủi ro cao).
- Cài đặt `graph_utils.py` → Kết quả: Hàm `is_valid` và `get_supplementary_docs` hỗ trợ lọc văn bản.
- Viết unit test cho Quality Gate 1 → Kết quả: `tests/test_data_quality.py` (Passed 100%).
- Cập nhật `session_state.md` và `CLAUDE.local.md` để kết thúc Phase 1.

### Đã bắt đầu nhưng chưa xong 🟡
- Đã chuẩn bị kiến trúc và logic cho Phase 2 (Retrieval Pipeline). Sắp tới sẽ làm việc với `qdrant_setup.py` và `embedding.py`.

## 3. Việc cần làm TIẾP THEO (ưu tiên cao → thấp)

1. **Setup Qdrant & Embedding BGE-M3** — Agent: RAG Engineer Agent
   - Làm thế nào: Sử dụng Qdrant local và HuggingFace BGE-M3 model để vector hóa toàn bộ chunks.
   - File input: `data/chunks.jsonl`
   - Output mong muốn: `backend/retrieval/qdrant_setup.py` & `backend/retrieval/embedding.py`

## 4. Quyết định quan trọng đã chốt trong session này

| Quyết định | Lý do |
|---|---|
| Graph Expansion & Rule-based extraction | Sử dụng thư viện Regex tách context (giới hạn bằng `re.split` tại dấu chấm/phẩy) để chống rò rỉ ngữ cảnh hiệu quả nhất cho pháp luật VN, thay vì dùng LLM tốn kém hoặc fixed window dễ lỗi. |
| Quality Gate 1 | Chỉ nghiệm thu Phase 1 khi các testcases kiểm tra tính hợp lệ chunk schema và relation schema Passed 100%. Đã đạt yêu cầu. |

## 5. Vấn đề / Blocker cần giải quyết

- (Không có blocker hiện tại). Đã sẵn sàng làm Phase 2.

## 6. Tài nguyên liên quan

| Tài liệu | Đường dẫn | Lý do liên quan |
|---|---|---|
| Báo cáo Phase 1 | `brain/.../walkthrough.md` | Tóm tắt kiến trúc Rule-based relation của Phase 1 |
| Document Relations | `data/document_relations.json` | Knowledge graph cho RAG expansion |

## 7. Prompt để bootstrap conversation mới

Paste đoạn sau vào đầu conversation mới:

---
Đọc các file theo thứ tự:
1. `.claude/CLAUDE.local.md` — phase hiện tại, bản đồ tài liệu
2. `.claude/project/session_state.md` — task chi tiết, quyết định đã chốt
3. `.claude/project/handoff_2026-09-16.md` — tóm tắt session vừa rồi

Sau đó tiếp tục từ: **Setup Qdrant & Embedding BGE-M3**
---
