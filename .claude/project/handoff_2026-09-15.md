# Handoff Summary — 2026-09-15 23:00

## 1. Tình trạng hiện tại (1 câu)
> Phase 1 — Data Foundation. Vừa hoàn tất: Phân tích nguồn dữ liệu, test thành công script trích xuất `.docx` và xác định phương án tải thủ công 19 file Word còn lại từ thuvienphapluat.vn. Đang dở: Chờ người dùng hoàn tất tải và đổi tên file `.docx`.

## 2. Việc đã làm trong session này

### Hoàn tất ✅
- Lập danh sách 30 VB cần crawl → `data/crawl_plan.md` (và cập nhật danh sách hướng dẫn tải thủ công tại `implementation_plan.md`).
- Crawl tự động thành công 4 file (BLLĐ 2019, BHYT, NĐ 143/2018, NĐ 39/2016) từ HuggingFace trước khi dataset bị khóa. File `.md` lưu tại `data/raw/hf/`.
- Tìm ra nguyên nhân lỗi pipeline cũ tạo ra 33 file `.json` rỗng trong `data/cleaned/` (do chạy parser trên file PDF scan). Đã dọn dẹp thư mục này.
- Phân loại 5 file PDF scan cũ của người dùng vào `data/raw/pdf_scan/yeu_cau/`. Quyết định không dùng OCR cho các file này mà ưu tiên bản Word.
- Test script đọc thành công file `.docx` (145/2020) với dữ liệu siêu sạch bằng thư viện `python-docx`.

### Đã bắt đầu nhưng chưa xong 🟡
- Thu thập 20 văn bản thủ công (Phương án C).
  - Người dùng đang tải các file `.docx` từ `thuvienphapluat.vn` lưu vào `data/raw/manual/`.

## 3. Việc cần làm TIẾP THEO (ưu tiên cao → thấp)

1. **Viết script Ingestion cho file .docx** — Agent: Ingestion Agent
   - Làm thế nào: Sử dụng `python-docx` để extract text, làm sạch và chuyển sang Markdown chuẩn (giống định dạng của 4 file `.md` đã có). 
   - File input: Các file Word tại `data/raw/manual/*.docx`
   - Output mong muốn: Các file markdown sạch được xử lý tự động.

2. **Hợp nhất và tiến hành Chunking** — Agent: Ingestion Agent
   - Làm thế nào: Sau khi có đủ text của các file `.md` (từ HF) và từ `.docx`, chuyển sang bước Parse + Clean → `data/cleaned/` và tiến hành Chunking theo Điều → `data/chunks.jsonl`.

## 4. Quyết định quan trọng đã chốt trong session này

| Quyết định | Lý do |
|---|---|
| Chuyển sang tải thủ công `.docx` | Dataset HF bị private, các nguồn web khác là SPA (chặn request tĩnh), PDF thì toàn là file có ảnh scan mộc đỏ. |
| Tuyệt đối không dùng OCR cho PDF scan | OCR có rủi ro sai lệch dấu câu, con số, cực kỳ nguy hiểm cho hệ thống Luật (Legal RAG). |

## 5. Vấn đề / Blocker cần giải quyết

- 🔴 **Blocker**: Hugging Face dataset `tmquan/vbpl-vn` đã chuyển private (HTTP 401). → Cần: Phụ thuộc vào người dùng tải thủ công 19 file Word còn thiếu.

## 6. Tài nguyên liên quan

| Tài liệu | Đường dẫn | Lý do liên quan |
|---|---|---|
| Hướng dẫn tải file | `implementation_plan.md` | Chứa danh sách link tải từng văn bản và quy tắc đặt tên các file `.docx` |
| Danh sách 30 VB | `data/crawl_plan.md` | Định hướng về danh sách các văn bản cốt lõi và mở rộng cần chuẩn bị |

## 7. Prompt để bootstrap conversation mới

Paste đoạn sau vào đầu conversation mới:

---
Đọc các file theo thứ tự:
1. `.claude/CLAUDE.local.md` — phase hiện tại, bản đồ tài liệu
2. `.claude/project/session_state.md` — task chi tiết, quyết định đã chốt
3. `.claude/project/handoff_2026-09-15.md` — tóm tắt session vừa rồi

Sau đó tiếp tục từ: **Viết script Ingestion cho file .docx** (chờ báo cáo tải xong từ user).
---
